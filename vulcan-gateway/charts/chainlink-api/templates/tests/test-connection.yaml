apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chainlink-api.fullname" . }}-test-connection"
  labels:
    {{- include "chainlink-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chainlink-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
