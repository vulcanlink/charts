apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "parity.fullname" . }}-test-connection"
  labels:
    {{- include "parity.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "parity.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
