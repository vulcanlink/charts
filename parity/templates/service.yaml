apiVersion: v1
kind: Service
metadata:
  name: {{ include "parity.fullname" . }}
  labels:
    {{- include "parity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: tcp
      protocol: TCP
      name: tcp
  selector:
    {{- include "parity.selectorLabels" . | nindent 4 }}
